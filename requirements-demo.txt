# Integrated demo pipeline - FALCONPRD, python 3.12, stub-detector mode.
#
# Constraints are lower bounds with upper guards only where a major version is
# known to change behaviour we depend on. They are BEST EFFORT - they have not
# been resolved on a machine with network access. Verify with:
#     python tools/preflight.py --skip-gpu
# after installing, and re-pin anything that resolves badly.

# ── Core image + data ────────────────────────────────────────────────────────
numpy>=1.26,<3
pillow>=10.2
pandas>=2.2
requests>=2.31

# opencv-python-headless, NOT opencv-python. FALCONPRD is a server; the non
# headless wheel links libGL.so.1 and friends, which are usually absent on a
# headless host and fail at `import cv2` with an OSError, not a pip error.
# Both wheels provide the same `cv2` module, so quality_check.py cannot tell.
opencv-python-headless>=4.9

# pyyaml carries TWO things now, and its status has changed accordingly.
#
# It always held mode 3's tuned system prompts (config/prompts.yaml), which was
# a soft dependency: PromptStore degrades to the built-ins and says why.
#
# It now ALSO holds the question registry - config/domains.yaml, classes.yaml
# and questions/*.yaml. Without it the registry cannot read any of them and
# falls back to the two built-in Site Safety questions, naming the reason in
# every UI's status line. The demo still starts; it just has 2 questions instead
# of 16 and no Infra domain at all. Treat this as required.
pyyaml>=6.0

# ── UI ───────────────────────────────────────────────────────────────────────
# The integrated demo UI (app/demo_dash.py). Dash 3 renamed app.run_server to
# app.run; this app uses app.run, which exists in 2.16+ as well.
dash>=2.16
# Held below 6 deliberately. gradio 6 moved css=/head= from the Blocks
# constructor to launch(); vlm_test_client_v2._theme_kwargs() handles both, but
# a single 4/5-series pin is one less thing to be wrong on stage. gr.skip(),
# which the batch UI's _emit() depends on, exists from 4.x onward.
gradio>=4.44,<6

# ── Stage 1 segmentation ─────────────────────────────────────────────────────
# rembg pulls a large transitive set (scipy, scikit-image, pymatting, pooch)
# for alpha matting we never use - get_foreground_box() calls remove() with
# only_mask=True. If the resolve fights you, see SETUP.md for the slim path.
rembg>=2.0.57
onnxruntime>=1.17

# ── Stage 2b: OCR ────────────────────────────────────────────────────────────
# PP-OCRv6 through RapidOCR's onnxruntime backend, over the ONNX files committed
# in models/ocr/. Needed by the four questions whose YAML sets ocr.enabled:
# the two SPD "installed" questions and the two device readings.
#
# SOFT, like rembg is not: without it stage 2b is skipped, the UI says so in
# one line, and those four questions are answered from the image alone. That is
# a legitimate way to run the demo, just a weaker one - so preflight warns
# rather than fails.
#
# rapidocr pulls omegaconf, which pulls antlr4-python3-runtime - an sdist that
# does NOT build against some distro-patched setuptools (it fails on
# `install_layout`). If that is what the resolve hits, build it inside a clean
# venv rather than against the system interpreter.
#
# onnxruntime is already required above for rembg; RapidOCR uses the same one.
rapidocr>=2.0

# ── Trained detector (app/demo_dash_yolox.py only) ───────────────────────────
# Not needed by the annotation-file version. CPU wheels are the right choice
# here: a YOLOX-S forward pass takes well under the ~2s the quality stage
# already costs, so the GPU build's size and driver-matching are not worth it.
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# torch>=2.2
# torchvision>=0.17
# Vendored yolox/utils imports these at module scope:
# loguru>=0.7
# psutil>=5.9
